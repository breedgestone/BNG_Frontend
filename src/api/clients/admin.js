/**
 * src/api/clients/admin.js
 * Admin API client
 */
import api from '@/api/http'

export const adminClient = {
  // Add your admin endpoints here
  // Example:
  // getAllAdmins(params) {
  //   return api.get('/api/v1/admins', { params }).then((r) => r.data)
  // },
}
