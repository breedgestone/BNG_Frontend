/**
 * src/api/clients/auth.js
 * Authentication API client
 */
import api from '@/api/http'

export const authClient = {
  // Add your authentication endpoints here
  // Example:
  // login(payload) {
  //   return api.post('/api/v1/auth/login', payload).then((r) => r.data)
  // },
}
