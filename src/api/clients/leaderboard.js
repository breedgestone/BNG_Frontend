/**
 * src/api/clients/leaderboard.js
 * Leaderboard API client
 */
import api from '@/api/http'

export const leaderboardClient = {
  // Add your leaderboard endpoints here
  // Example:
  // getLeaderboard(params) {
  //   return api.get('/api/v1/leaderboard', { params }).then((r) => r.data)
  // },
}
