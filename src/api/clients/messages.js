/**
 * src/api/clients/messages.js
 * Messages API client
 */
import api from '@/api/http'

export const messagesClient = {
  // Add your messages endpoints here
  // Example:
  // getAllMessages(params) {
  //   return api.get('/api/v1/messages', { params }).then((r) => r.data)
  // },
}
