/**
 * src/api/clients/payouts.js
 * Payouts API client
 */
import api from '@/api/http'

export const payoutsClient = {
  // Add your payouts endpoints here
  // Example:
  // getAllPayouts(params) {
  //   return api.get('/api/v1/payouts', { params }).then((r) => r.data)
  // },
}
