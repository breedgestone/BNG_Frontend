/**
 * src/api/clients/promotions.js
 * Promotions API client
 */
import api from '@/api/http'

export const promotionsClient = {
  // Add your promotions endpoints here
  // Example:
  // getAllPromotions(params) {
  //   return api.get('/api/v1/promotions', { params }).then((r) => r.data)
  // },
}
