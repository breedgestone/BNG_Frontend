/**
 * src/api/clients/reports.js
 * Reports API client
 */
import api from '@/api/http'

export const reportsClient = {
  // Add your reports endpoints here
  // Example:
  // getReports(params) {
  //   return api.get('/api/v1/reports', { params }).then((r) => r.data)
  // },
}
