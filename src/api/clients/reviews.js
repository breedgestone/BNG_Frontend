/**
 * src/api/clients/reviews.js
 * Customer Reviews API client
 */
import api from '@/api/http'

export const reviewsClient = {
  // Add your reviews endpoints here
  // Example:
  // getAllReviews(params) {
  //   return api.get('/api/v1/reviews', { params }).then((r) => r.data)
  // },
}
