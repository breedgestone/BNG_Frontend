/**
 * src/api/clients/schedule.js
 * Schedule API client
 */
import api from '@/api/http'

export const scheduleClient = {
  // Add your schedule endpoints here
  // Example:
  // getSchedule(params) {
  //   return api.get('/api/v1/schedule', { params }).then((r) => r.data)
  // },
}
