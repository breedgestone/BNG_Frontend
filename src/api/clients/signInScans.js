/**
 * src/api/clients/signInScans.js
 * Sign In Scans API client
 */
import api from '@/api/http'

export const signInScansClient = {
  // Add your sign in scans endpoints here
  // Example:
  // getAllScans(params) {
  //   return api.get('/api/v1/scans', { params }).then((r) => r.data)
  // },
}
