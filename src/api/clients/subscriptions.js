/**
 * src/api/clients/subscriptions.js
 * Subscriptions API client
 */
import api from '@/api/http'

export const subscriptionsClient = {
  // Add your subscriptions endpoints here
  // Example:
  // getAllSubscriptions(params) {
  //   return api.get('/api/v1/subscriptions', { params }).then((r) => r.data)
  // },
}
