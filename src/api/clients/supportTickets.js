/**
 * src/api/clients/supportTickets.js
 * Support Tickets API client
 */
import api from '@/api/http'

export const supportTicketsClient = {
  // Add your support tickets endpoints here
  // Example:
  // getAllTickets(params) {
  //   return api.get('/api/v1/support-tickets', { params }).then((r) => r.data)
  // },
}
