/**
 * src/api/clients/workouts.js
 * Workouts API client
 */
import api from '@/api/http'

export const workoutsClient = {
  // Add your workouts endpoints here
  // Example:
  // getAllWorkouts(params) {
  //   return api.get('/api/v1/workouts', { params }).then((r) => r.data)
  // },
}
